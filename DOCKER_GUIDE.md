# 🐳 Руководство по запуску с Docker

## Быстрый старт

### 1. Сборка и запуск с помощью docker-compose (рекомендуется)

```bash
# Собрать образ и запустить контейнер
docker-compose up --build

# Запустить в фоновом режиме
docker-compose up -d

# Остановить контейнер
docker-compose down
```

### 2. Запуск с помощью Docker напрямую

```bash
# Собрать образ
docker build -t fastapi-log-analyzer .

# Запустить контейнер
docker run -d -p 8001:8001 --name log-analyzer fastapi-log-analyzer

# Остановить контейнер
docker stop log-analyzer

# Удалить контейнер
docker rm log-analyzer
```

## Доступ к приложению

После запуска приложение будет доступно по адресу:
- **Главная страница**: http://localhost:8001
- **Дашборд**: http://localhost:8001/dashboard
- **API документация**: http://localhost:8001/docs

## Разработка с hot-reload

Для разработки с автоматической перезагрузкой при изменении кода:

1. Раскомментируйте в `docker-compose.yml` строку с монтированием кода:
```yaml
volumes:
  - .:/app
```

2. Измените команду запуска в `Dockerfile`:
```dockerfile
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8001", "--reload"]
```

3. Перезапустите контейнер:
```bash
docker-compose up --build
```

## Поддержка GPU (NVIDIA CUDA)

Если у вас есть NVIDIA GPU и установлен [NVIDIA Container Toolkit](https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/install-guide.html):

1. Установите PyTorch с CUDA поддержкой в `requirements.txt`:
```
torch>=2.0.0
```

2. Раскомментируйте секцию GPU в `docker-compose.yml`:
```yaml
deploy:
  resources:
    reservations:
      devices:
        - driver: nvidia
          count: 1
          capabilities: [gpu]
```

3. Перезапустите контейнер:
```bash
docker-compose up --build
```

## Полезные команды

```bash
# Просмотр логов
docker-compose logs -f

# Просмотр запущенных контейнеров
docker ps

# Зайти внутрь контейнера
docker exec -it fastapi-log-analyzer bash

# Очистить все Docker ресурсы
docker system prune -a

# Пересобрать образ без кеша
docker-compose build --no-cache
```

## Переменные окружения

Вы можете добавить дополнительные переменные окружения в `docker-compose.yml`:

```yaml
environment:
  - PYTHONUNBUFFERED=1
  - LOG_LEVEL=info
  - MAX_WORKERS=4
```

## Требования

- Docker версии 20.10 или выше
- Docker Compose версии 2.0 или выше
- (Опционально) NVIDIA Container Toolkit для GPU поддержки

## Устранение проблем

### Порт уже занят
Если порт 8001 занят, измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8002:8001"  # Используем порт 8002 вместо 8001
```

### Проблемы с правами доступа
Если возникают проблемы с правами, измените UID пользователя в `Dockerfile`:
```dockerfile
RUN useradd -m -u $(id -u) appuser
```

### Медленная сборка
Используйте BuildKit для ускорения:
```bash
DOCKER_BUILDKIT=1 docker-compose build
```

## Продакшн деплой

Для продакшн окружения:

1. Удалите флаг `--reload` из команды uvicorn
2. Используйте gunicorn с uvicorn workers:
```dockerfile
CMD ["gunicorn", "main:app", "-w", "4", "-k", "uvicorn.workers.UvicornWorker", "--bind", "0.0.0.0:8001"]
```
3. Добавьте gunicorn в `requirements.txt`
4. Настройте reverse proxy (nginx, traefik)

