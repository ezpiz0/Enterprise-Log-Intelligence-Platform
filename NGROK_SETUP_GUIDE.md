# 🚀 Инструкция: Настройка ngrok для Публичного Доступа

**Цель:** Сделать ваш сервер доступным для друзей через интернет

---

## 📥 Шаг 1: Установка ngrok

### Windows (Рекомендуемый способ):

#### Вариант A: Через Chocolatey (если установлен)
```powershell
choco install ngrok
```

#### Вариант B: Ручная установка (РЕКОМЕНДУЕТСЯ)

1. **Скачайте ngrok:**
   - Перейдите на https://ngrok.com/download
   - Скачайте версию для Windows (64-bit)
   - Файл: `ngrok-v3-stable-windows-amd64.zip`

2. **Распакуйте архив:**
   - Распакуйте `ngrok.exe` в удобную папку
   - Рекомендую: `C:\ngrok\` или `D:\ngrok\`

3. **Добавьте в PATH (опционально, но удобно):**
   ```powershell
   # Запустите PowerShell как Администратор
   [Environment]::SetEnvironmentVariable("Path", $env:Path + ";C:\ngrok", "User")
   ```
   
   Или вручную:
   - Правой кнопкой на "Этот компьютер" → Свойства
   - Дополнительные параметры системы
   - Переменные среды
   - В Path добавьте путь к папке с ngrok.exe

---

## 🔑 Шаг 2: Добавление Authtoken

После установки ngrok выполните:

```powershell
# Если ngrok в PATH:
ngrok config add-authtoken 33xJiXbggbf0VkJQYYS0Q1Rt7IF_4N9bEXBDXbBDvzMjvEPLy

# Или укажите полный путь:
C:\ngrok\ngrok.exe config add-authtoken 33xJiXbggbf0VkJQYYS0Q1Rt7IF_4N9bEXBDXbBDvzMjvEPLy
```

**Ожидаемый результат:**
```
Authtoken saved to configuration file: C:\Users\YourName\.ngrok2\ngrok.yml
```

---

## 🚀 Шаг 3: Запуск Сервера

### Вариант 1: Через Python (Рекомендуется для тестирования)

```powershell
# В директории проекта
python main.py
```

**Вы должны увидеть:**
```
INFO:     Uvicorn running on http://127.0.0.1:8001
```

### Вариант 2: Через Docker

```powershell
docker-compose up -d
```

---

## 🌐 Шаг 4: Запуск ngrok

**Откройте НОВЫЙ терминал** (не закрывайте окно с сервером!) и выполните:

```powershell
# Если ngrok в PATH:
ngrok http 8001

# Или полный путь:
C:\ngrok\ngrok.exe http 8001
```

⚠️ **Важно:** Используйте порт **8001**, а не 80!

---

## 📊 Шаг 5: Получение Публичного URL

После запуска ngrok вы увидите:

```
ngrok

Session Status                online
Account                       your_account@email.com
Version                       3.x.x
Region                        United States (us)
Latency                       50ms
Web Interface                 http://127.0.0.1:4040

Forwarding                    https://abc123xyz.ngrok-free.app -> http://localhost:8001

Connections                   ttl     opn     rt1     rt5     p50     p90
                              0       0       0.00    0.00    0.00    0.00
```

### 🎉 Ваш Публичный URL:
```
https://abc123xyz.ngrok-free.app
```

**Отправьте эту ссылку друзьям!** 🚀

---

## 🧪 Шаг 6: Проверка Работы

### Локальная проверка:
1. Откройте браузер
2. Перейдите на `http://localhost:8001`
3. Убедитесь, что сервер работает

### Публичная проверка:
1. Откройте браузер
2. Перейдите на ваш ngrok URL: `https://abc123xyz.ngrok-free.app`
3. Вы должны увидеть главную страницу вашего приложения

**Друзья могут:**
- Открыть эту же ссылку
- Загружать архивы с логами
- Просматривать результаты анализа

---

## 📱 Web Interface (Дополнительно)

ngrok предоставляет веб-интерфейс для мониторинга:

```
http://127.0.0.1:4040
```

Здесь вы увидите:
- Все запросы к вашему серверу
- Детали каждого запроса
- Статистику подключений

---

## ⚠️ Важные Замечания

### 1. Бесплатный Plan
- ✅ Один туннель одновременно
- ✅ 40 подключений в минуту
- ⚠️ URL меняется при каждом перезапуске
- ⚠️ На ngrok URL может появиться предупреждение (нажмите "Visit Site")

### 2. Сервер Должен Работать
```
Терминал 1: python main.py     ← Должен быть запущен
Терминал 2: ngrok http 8001    ← Должен быть запущен
```

Если закроете любой из них - доступ пропадет!

### 3. Безопасность
- ⚠️ Любой с URL может зайти на ваш сервер
- ⚠️ Не оставляйте сервер запущенным без контроля
- ✅ Закройте ngrok когда закончите: `Ctrl+C`

---

## 🛠️ Быстрые Команды

### Запуск Всего (2 терминала):

**Терминал 1:**
```powershell
cd D:\Downloads\FASTAPIGITPROJECT
python main.py
```

**Терминал 2:**
```powershell
ngrok http 8001
```

### Остановка:
```
Ctrl+C в обоих терминалах
```

---

## 🐛 Решение Проблем

### Проблема: "ngrok не найден"
**Решение:** Укажите полный путь:
```powershell
C:\ngrok\ngrok.exe http 8001
```

### Проблема: "Failed to start tunnel"
**Решение:** 
1. Проверьте, что сервер запущен: `http://localhost:8001`
2. Проверьте authtoken
3. Попробуйте другой регион: `ngrok http 8001 --region=eu`

### Проблема: "Too many connections"
**Решение:** 
- Бесплатный план ограничен
- Подождите минуту
- Или обновитесь до платного плана

### Проблема: "This site can't be reached"
**Решение:**
1. Убедитесь, что оба терминала запущены
2. Проверьте URL (он меняется каждый раз)
3. Проверьте локально: `http://localhost:8001`

---

## 🎯 Проверочный Чек-лист

Перед тем как давать ссылку друзьям:

- [ ] ngrok установлен
- [ ] Authtoken добавлен
- [ ] Сервер запущен (python main.py)
- [ ] ngrok запущен (ngrok http 8001)
- [ ] Получен публичный URL
- [ ] Локально работает (localhost:8001)
- [ ] По ngrok URL работает
- [ ] Друзья могут зайти

---

## 📞 Следующие Шаги

1. **Установите ngrok** (если ещё не сделали)
2. **Запустите два терминала** (сервер + ngrok)
3. **Скопируйте URL** из вывода ngrok
4. **Отправьте друзьям** 🎉

---

## 💡 Альтернатива (Если ngrok не работает)

### localtunnel
```powershell
npm install -g localtunnel
lt --port 8001
```

### serveo
```powershell
ssh -R 80:localhost:8001 serveo.net
```

---

**Готово!** Теперь ваши друзья смогут подключиться к вашему серверу! 🚀

*Инструкция создана: 12 октября 2025*





