# Time Machine - Руководство по использованию

## 📖 Описание

**Time Machine (Реконструкция Инцидента)** — это функция интерактивного дашборда AIOps, которая позволяет пользователям "путешествовать во времени" по загруженным данным логов, наблюдая динамику развития инцидентов.

## ✨ Возможности

### 1. **Глобальный Timeline Scrubber**
- Расположен внизу дашборда (фиксированная позиция)
- Автоматически появляется после загрузки данных
- Отображает текущее выбранное время в читаемом формате `YYYY-MM-DD HH:mm:ss`

### 2. **Интерактивный ползунок (Slider)**
- Диапазон автоматически определяется по min/max временным меткам из логов
- Плавное изменение времени с визуальной индикацией прогресса
- Дебаунс 200мс для оптимизации производительности

### 3. **Фильтрация данных по времени**
- Показывает только события (ERROR и WARNING), произошедшие **ДО или В МОМЕНТ** выбранного времени
- Все компоненты дашборда динамически обновляются:
  - **KPI-карточки** (Всего инцидентов, Найдено аномалий, Прогнозы, Затронуто систем)
  - **График "Динамика инцидентов"**
  - **График "Проблемы по влиянию"**
  - **График "Затронутые системы"**
  - **Таблица "Критические инциденты"**

### 4. **Функция воспроизведения (Play/Pause)**
- **▶ PLAY**: Автоматическое движение ползунка от текущей позиции к концу диапазона
- **⏸ PAUSE**: Остановка автоматического воспроизведения
- Скорость: 50 шагов для полного прохода, обновление каждые 500мс
- При достижении конца таймлайна автоматически останавливается

## 🚀 Как использовать

### Шаг 1: Загрузите данные
1. Перейдите на главную страницу
2. Загрузите ZIP-архив с логами
3. Дождитесь завершения обработки

### Шаг 2: Откройте дашборд
- После обработки вы автоматически попадете на дашборд
- Time Machine появится внизу экрана

### Шаг 3: Используйте Time Machine

#### Ручной режим:
1. Переместите ползунок влево (назад во времени)
2. Наблюдайте изменения на всех графиках и в таблицах
3. Текущее время отображается над ползунком

#### Режим воспроизведения:
1. Установите ползунок в начальную позицию (или он автоматически вернется к началу)
2. Нажмите **▶ PLAY**
3. Наблюдайте автоматическую "прокрутку" инцидентов
4. Нажмите **⏸ PAUSE** для остановки в любой момент

## 🎯 Примеры использования

### Пример 1: Анализ развития инцидента
1. Переместите ползунок к началу временного диапазона
2. Медленно двигайте вправо, наблюдая:
   - Как растет количество ошибок на графике "Динамика инцидентов"
   - Как увеличивается impact score в графике "Проблемы по влиянию"
   - Какие системы затрагиваются первыми

### Пример 2: Поиск корневой причины
1. Установите время на момент пика ошибок (из графика "Динамика инцидентов")
2. Переместитесь на 5-10 минут назад
3. Изучите, какие WARNING появились перед критическим сбоем

### Пример 3: Демонстрация инцидента
1. Нажмите **▶ PLAY**
2. Покажите заинтересованным лицам динамическое развитие инцидента
3. Остановитесь на ключевых моментах для детального объяснения

## 🛠 Технические детали

### Архитектура
```javascript
// Глобальное состояние
timeMachineState = {
    enabled: false,              // Активирован ли Time Machine
    minTime: null,               // Минимальная временная метка
    maxTime: null,               // Максимальная временная метка
    currentActiveTime: null,     // Текущее выбранное время
    isPlaying: false,            // Режим воспроизведения
    playInterval: null,          // Интервал для анимации
    allTimestamps: []            // Все временные метки из данных
}
```

### Механизм фильтрации
```javascript
function filterDataByTime(data) {
    const cutoffTime = timeMachineState.currentActiveTime;
    
    // Фильтруем submit (ERROR логи)
    const filteredSubmit = data.submit.filter(row => {
        const timestamp = parseTimestamp(row['Строка из лога']);
        return timestamp && timestamp <= cutoffTime;
    });
    
    // Фильтруем predictions
    const filteredPredictions = data.predictions.filter(row => {
        const timestamp = new Date(row['Время триггера']);
        return timestamp <= cutoffTime;
    });
    
    return { submit, predictions, ... };
}
```

### Оптимизация производительности
- **Debounce 200мс** на обработчик изменения ползунка
- Разделение событий `input` (плавное UI) и `change` (перерисовка)
- Пропуск повторной инициализации Time Machine при обновлении дашборда

## 🎨 Дизайн

### Стиль
- **Neon-эффекты** в соответствии с общей темой дашборда
- **Анимированный прогресс-бар** с градиентом от cyan до red
- **Hover-эффекты** на ползунке (увеличение размера, усиление свечения)
- **Responsive дизайн** для мобильных устройств

### Цветовая схема
- Основной цвет: `var(--neon-cyan)` (#FF994E)
- Критический цвет: `var(--critical)` (#FF4136)
- Фон: `rgba(18, 18, 18, 0.85)` с backdrop-filter blur

## 📊 Поддерживаемые источники данных

Time Machine автоматически извлекает временные метки из:
1. **submit_report.xlsx** → поле `"Строка из лога"` (ERROR логи)
2. **predictive_alerts.xlsx** → поле `"Время триггера"` (прогнозы)

Формат timestamp: `YYYY-MM-DDTHH:mm:ss` (ISO 8601)

## 🐛 Устранение неполадок

### Time Machine не появляется
- **Причина**: В данных нет временных меток
- **Решение**: Убедитесь, что логи содержат timestamp в формате ISO 8601

### Ползунок не двигается плавно
- **Причина**: Низкая производительность браузера
- **Решение**: Закройте лишние вкладки, увеличьте debounce до 300-500мс

### Графики не обновляются
- **Причина**: Ошибка в консоли браузера
- **Решение**: Откройте DevTools (F12) и проверьте наличие ошибок JavaScript

## 🔮 Будущие улучшения

- [ ] Маркеры критических событий на таймлайне
- [ ] Возможность выбора нескольких временных точек для сравнения
- [ ] Экспорт "снимка" состояния системы в выбранный момент времени
- [ ] Регулируемая скорость воспроизведения (1x, 2x, 5x)
- [ ] Закладки (bookmarks) для важных моментов времени

## 📞 Поддержка

По вопросам и предложениям обращайтесь:
- Email: support@atomichack.example
- GitHub: [Создать Issue](https://github.com/yourorg/aiops-dashboard/issues)

---

**Версия**: 1.0.0  
**Дата релиза**: 2025-10-11  
**Совместимость**: AIOps Dashboard v2.0.0+

