# ✅ ФИНАЛЬНАЯ СТРУКТУРА ФАЙЛОВ

## 📊 novel_anomalies.xlsx - НОВАЯ СТРУКТУРА (v3.0)

### 🎯 Формат идентичен submit_report.xlsx!

Файл содержит WARNING-логи, которые не найдены в базе знаний, но коррелируют с известными проблемами.

### 📋 Структура колонок:

| № | Колонка | Тип | Описание | Пример |
|---|---------|-----|----------|--------|
| 1 | **ID сценария** | string | Идентификатор кейса | `"1"` |
| 2 | **ID аномалии** | integer | ID аномалии (0 = новая) | `0` |
| 3 | **ID проблемы** | integer | ID связанной проблемы | `1`, `34`, `41` |
| 4 | **Файл с проблемой** | string | Имя файла с WARNING | `"app_server_log.txt"` |
| 5 | **№ строки** | integer | Номер строки в файле | `142` |
| 6 | **Строка из лога** | string | **Полный лог с timestamp!** | `"2024-10-11T08:15:30 WARNING: Database timeout"` |

---

## 🔥 Ключевые особенности:

### ✅ Полностью аналогично submit_report.xlsx
- Одинаковые названия колонок
- Одинаковые типы данных
- Одинаковый формат timestamp в логе

### ✅ Timestamp в поле "Строка из лога"
- Формат: `YYYY-MM-DDTHH:mm:ss WARNING: ...`
- Парсится функцией `parseTimestamp()` в JavaScript
- Автоматически отображается на графике Time Machine

### ✅ ID аномалии = 0
- Означает "новая аномалия, не в базе знаний"
- Отличает от известных аномалий (ID > 0)

### ✅ Полная трассируемость
- Точный файл и номер строки
- Связь с конкретной проблемой (ID)
- Полный текст лога с параметрами

---

## 📈 Пример записи:

```json
{
  "ID сценария": "1",
  "ID аномалии": 0,
  "ID проблемы": 1,
  "Файл с проблемой": "app_server_log.txt",
  "№ строки": 142,
  "Строка из лога": "2024-10-11T08:15:30 WARNING: Database connection timeout after 30s"
}
```

---

## 🔧 JavaScript код (одинаковый для errors и anomalies):

```javascript
// Для ошибок из submit_report.xlsx
data.submit.forEach(row => {
    const errorLog = row['Строка из лога'];
    const timestamp = parseTimestamp(errorLog);  // Извлекает 2024-10-11T08:15:30
    if (timestamp) {
        // Используем для графика
    }
});

// Для аномалий из novel_anomalies.xlsx (ИДЕНТИЧНО!)
data.novel.forEach(row => {
    const anomalyLog = row['Строка из лога'];
    const timestamp = parseTimestamp(anomalyLog);  // Извлекает 2024-10-11T08:15:30
    if (timestamp) {
        // Используем для графика
    }
});
```

---

## 🎨 Отображение на дашборде:

### График "Динамика инцидентов":
- 🔴 **ОШИБКИ** (из submit_report.xlsx) - красная линия
- 💛 **ПРОГНОЗЫ** (из predictive_alerts.xlsx) - желтая линия, пунктир
- 🟠 **АНОМАЛИИ** (из novel_anomalies.xlsx) - оранжевая линия ← **РАБОТАЕТ!**

### KPI-карточки:
- "Всего Инцидентов" - считает из submit_report.xlsx
- "Найдено Аномалий" - считает из submit_report.xlsx (WARNING)
- **Новые аномалии не считаются отдельно**, так как это просто WARNING без match в базе знаний

### Time Machine:
- ✅ Фильтрует ошибки по времени
- ✅ Фильтрует прогнозы по времени
- ✅ Фильтрует аномалии по времени ← **РАБОТАЕТ!**

---

## 📊 Сравнение с предыдущей структурой:

### ❌ Старая структура (v1.0-v2.0):
```
ID сценария | Тип Алерта | Время WARNING | Текст | Лог | Файл WARNING | ...
```
- Много лишних полей
- Отдельное поле для timestamp
- Сложная структура

### ✅ Новая структура (v3.0):
```
ID сценария | ID аномалии | ID проблемы | Файл с проблемой | № строки | Строка из лога
```
- Минимум полей
- Timestamp в самом логе (как в submit_report)
- Простая и понятная структура

---

## 🚀 Преимущества новой структуры:

1. **Единообразие**: Все файлы (submit_report, novel_anomalies) имеют одинаковую структуру
2. **Простота**: Меньше полей = проще работать
3. **Совместимость**: Один JavaScript код для обработки errors и anomalies
4. **Надежность**: Timestamp всегда есть в поле "Строка из лога"
5. **Трассируемость**: Точный файл и номер строки

---

## 🔄 Изменения в коде:

### Backend (Python):
```python
# report_generator.py - функция identify_novel_anomalies()
alert = {
    'ID сценария': scenario_id,
    'ID аномалии': 0,  # 0 = новая аномалия
    'ID проблемы': closest_error['final_problem_id'],
    'Файл с проблемой': warning.get('file_name', ''),
    '№ строки': warning.get('line_number', 0),
    'Строка из лога': warning['log']  # Полный лог с timestamp!
}
```

### Frontend (JavaScript):
```javascript
// dashboard.html - функция renderTimelineChart()
// АНАЛОГИЧНО submit_report: парсим timestamp из поля 'Строка из лога'
const anomalyLog = row['Строка из лога'];
const timestamp = parseTimestamp(anomalyLog);
if (timestamp) {
    timePoints.get(hourKey).anomalies++;
}
```

---

## ✅ Проверка работы:

### Шаг 1: Загрузите данные
1. Откройте http://127.0.0.1:8001/
2. Выберите модель (Light)
3. Загрузите ZIP-архив
4. Дождитесь обработки

### Шаг 2: Проверьте консоль (F12)
Должны увидеть:
```
=== ANOMALIES DEBUG ===
Total anomalies: 5
Available fields: ["ID сценария", "ID аномалии", "ID проблемы", "Файл с проблемой", "№ строки", "Строка из лога"]
First anomaly log: 2024-10-11T08:15:30 WARNING: ...
Parsed timestamp: Fri Oct 11 2024 08:15:30 GMT+0300
Found 5 anomalies with valid timestamps out of 5 total  ✅
```

### Шаг 3: Проверьте график
На графике "Динамика инцидентов" должны быть 3 линии с данными:
- 🔴 Красная (ERROR)
- 💛 Желтая (ПРОГНОЗЫ)
- 🟠 Оранжевая (АНОМАЛИИ) ← **5 точек!** ✨

### Шаг 4: Проверьте Excel
Скачайте результаты и откройте `novel_anomalies.xlsx`:
- ✅ 6 колонок (как в submit_report.xlsx)
- ✅ Поле "Строка из лога" содержит timestamp
- ✅ "ID аномалии" = 0 для всех записей
- ✅ Все данные корректны

---

## 📖 Версия документа
**Версия**: 3.0 FINAL  
**Дата**: 2025-10-12  
**Статус**: ✅ ГОТОВО К ИСПОЛЬЗОВАНИЮ  
**Совместимость**: AIOps Dashboard v2.0.0+

---

## 🎉 ИТОГО:

✅ Структура упрощена и унифицирована  
✅ Timestamp парсится из лога (как в submit_report)  
✅ График отображает аномалии корректно  
✅ Time Machine работает со всеми типами данных  
✅ Код протестирован и готов к использованию  

**ВСЁ РАБОТАЕТ! МОЖНО ПРОВЕРЯТЬ!** 🚀

