# ⚡ Быстрый старт с Docker

## 🚀 За 3 шага к работающему приложению

### Шаг 1: Установите Docker

<details>
<summary><b>Windows</b></summary>

1. Скачайте [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop)
2. Запустите установщик
3. Перезагрузите компьютер
4. Запустите Docker Desktop

</details>

<details>
<summary><b>macOS</b></summary>

1. Скачайте [Docker Desktop для Mac](https://www.docker.com/products/docker-desktop)
2. Установите приложение
3. Запустите Docker Desktop

</details>

<details>
<summary><b>Linux (Ubuntu/Debian)</b></summary>

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
sudo usermod -aG docker $USER
newgrp docker
```

</details>

---

### Шаг 2: Запустите приложение

#### 🎯 Самый простой способ

**Windows:**
```cmd
Дважды кликните на: docker-start.bat
```

**Linux/macOS:**
```bash
chmod +x docker-start.sh && ./docker-start.sh
```

#### 🔧 Альтернативный способ (через терминал)

```bash
docker-compose up -d
```

---

### Шаг 3: Откройте браузер

```
http://localhost:8001
```

---

## 🎯 Готово! Приложение работает!

Теперь вы можете:
- ✅ Загружать ZIP архивы с логами
- ✅ Выбирать модель анализа (быструю или точную)
- ✅ Получать результаты анализа
- ✅ Просматривать интерактивный дашборд

---

## 📋 Основные команды

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Логи (в реальном времени)
docker-compose logs -f

# Перезапуск
docker-compose restart

# Статус
docker-compose ps
```

---

## 🛑 Как остановить

**Windows:**
```cmd
Дважды кликните на: docker-stop.bat
```

**Linux/macOS:**
```bash
./docker-stop.sh
```

**Или через терминал:**
```bash
docker-compose down
```

---

## 📊 Проверка работы

После запуска проверьте:

1. **Контейнер запущен:**
```bash
docker ps
```
Должен быть контейнер `fastapi-log-analyzer`

2. **Приложение отвечает:**
```bash
curl http://localhost:8001/docs
```

3. **Логи без ошибок:**
```bash
docker-compose logs
```

---

## 🆘 Проблемы?

### "Docker не запущен"
→ Запустите Docker Desktop

### "Порт 8001 занят"
→ Измените порт в `docker-compose.yml`:
```yaml
ports:
  - "8002:8001"  # Используем 8002 вместо 8001
```

### "Недостаточно памяти"
→ Docker Desktop → Settings → Resources → увеличьте Memory до 4 GB

### Другие проблемы
→ Откройте `DOCKER_RU.md` - там подробный раздел по устранению проблем

---

## 📚 Дополнительная информация

- **Краткая инструкция:** `ЗАПУСК_DOCKER.txt`
- **Полное руководство (RU):** `DOCKER_RU.md`
- **Полное руководство (EN):** `DOCKER_GUIDE.md`
- **Список всех файлов:** `DOCKER_FILES.md`
- **Итоговая сводка:** `DOCKER_SUMMARY.md`

---

## 💡 Советы

1. **Для разработки** используйте обычный режим:
   ```bash
   docker-compose up
   ```

2. **Для продакшн** используйте фоновый режим:
   ```bash
   docker-compose up -d
   ```

3. **Для отладки** смотрите логи:
   ```bash
   docker-compose logs -f
   ```

4. **Для экономии места** периодически чистите Docker:
   ```bash
   docker system prune -a
   ```

---

<div align="center">

## ✨ Готово!

**Запустите:** `docker-compose up -d`

**Откройте:** http://localhost:8001

**Используйте:** Загрузите ZIP с логами и получите анализ!

---

**Если нужна помощь, читайте подробные инструкции в DOCKER_RU.md**

🐳 **Приятной работы с Docker!** 🚀

</div>

