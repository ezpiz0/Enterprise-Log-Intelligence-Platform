# 📦 Список Docker файлов и их назначение

## 🐳 Основные файлы Docker

### `Dockerfile`
Основной Dockerfile для разработки и тестирования.
- Простая структура
- Быстрая сборка
- Подходит для разработки

**Использование:**
```bash
docker build -t fastapi-log-analyzer .
```

---

### `Dockerfile.prod`
Оптимизированный Dockerfile для продакшн окружения.
- Multi-stage сборка
- Минимальный размер образа
- Health check
- Больше workers для uvicorn

**Использование:**
```bash
docker build -f Dockerfile.prod -t fastapi-log-analyzer:prod .
```

---

### `docker-compose.yml`
Основной файл для запуска контейнера в режиме разработки.
- Простая конфигурация
- Монтирование volumes для hot-reload (опционально)
- Переменные окружения

**Использование:**
```bash
docker-compose up
docker-compose up -d  # в фоновом режиме
```

---

### `docker-compose.prod.yml`
Файл для продакшн деплоя.
- Использует Dockerfile.prod
- Ограничения ресурсов
- Health checks
- Опционально nginx reverse proxy

**Использование:**
```bash
docker-compose -f docker-compose.prod.yml up -d
```

---

### `.dockerignore`
Исключает ненужные файлы из Docker образа.
- Документация (*.md)
- Кеш Python (__pycache__)
- Виртуальные окружения
- IDE конфигурации
- Временные файлы

**Результат:** Ускоряет сборку образа и уменьшает его размер.

---

### `.gitignore`
Исключает файлы из Git репозитория.
- Python кеш
- Виртуальные окружения
- Docker конфигурации (кроме примеров)
- Логи и временные файлы
- SSL сертификаты

---

## 📝 Конфигурационные файлы

### `nginx.conf.example`
Пример конфигурации Nginx для продакшн деплоя.
- HTTP → HTTPS редирект
- SSL конфигурация
- Reverse proxy для FastAPI
- Gzip сжатие
- Оптимизация статических файлов

**Использование:**
```bash
cp nginx.conf.example nginx.conf
# Отредактируйте nginx.conf под свои нужды
```

---

### `Makefile`
Упрощает работу с Docker командами.
- Короткие команды (make up, make down)
- Справка (make help)
- Автоматизация рутинных задач

**Использование:**
```bash
make help   # Показать все команды
make up     # Запустить
make logs   # Показать логи
make down   # Остановить
```

---

## 🖥️ Скрипты запуска

### Для Windows:

#### `docker-start.bat`
Скрипт запуска контейнера с интерактивным меню.
- Проверка установки Docker
- Выбор режима (обычный/фоновый)
- Автоматический запуск

**Использование:** Двойной клик или:
```cmd
docker-start.bat
```

#### `docker-stop.bat`
Останавливает контейнер.

**Использование:**
```cmd
docker-stop.bat
```

#### `docker-restart.bat`
Перезапускает контейнер.

**Использование:**
```cmd
docker-restart.bat
```

#### `docker-logs.bat`
Показывает логи контейнера в реальном времени.

**Использование:**
```cmd
docker-logs.bat
```

---

### Для Linux/macOS:

#### `docker-start.sh`
Скрипт запуска контейнера с интерактивным меню.

**Использование:**
```bash
chmod +x docker-start.sh
./docker-start.sh
```

#### `docker-stop.sh`
Останавливает контейнер.

**Использование:**
```bash
chmod +x docker-stop.sh
./docker-stop.sh
```

---

## 📚 Документация

### `DOCKER_GUIDE.md`
Полное руководство по Docker (на английском).
- Установка и настройка
- Все команды Docker
- Продакшн деплой
- GPU поддержка
- Troubleshooting

---

### `DOCKER_RU.md`
Полная инструкция по Docker на русском языке.
- Подробная установка для всех ОС
- Пошаговые инструкции
- Решение проблем
- GPU настройка

---

### `ЗАПУСК_DOCKER.txt`
Краткая инструкция для быстрого старта (на русском).
- Для Windows пользователей
- Для Linux/macOS пользователей
- Быстрые команды

---

### `DOCKER_FILES.md` (этот файл)
Описание всех Docker-related файлов в проекте.

---

## 🚀 Быстрая шпаргалка

### Первый запуск:
```bash
# Windows
docker-start.bat

# Linux/macOS
chmod +x docker-start.sh
./docker-start.sh

# Или напрямую
docker-compose up -d
```

### Остановка:
```bash
# Windows
docker-stop.bat

# Linux/macOS
./docker-stop.sh

# Или напрямую
docker-compose down
```

### Логи:
```bash
docker-compose logs -f
```

### Продакшн:
```bash
docker-compose -f docker-compose.prod.yml up -d
```

---

## 📊 Структура файлов

```
FASTAPIGITPROJECT/
│
├── 🐳 Docker конфигурация
│   ├── Dockerfile                     # Основной Dockerfile
│   ├── Dockerfile.prod                # Продакшн Dockerfile
│   ├── docker-compose.yml             # Docker Compose (dev)
│   ├── docker-compose.prod.yml        # Docker Compose (prod)
│   ├── .dockerignore                  # Исключения для Docker
│   └── nginx.conf.example             # Пример nginx конфигурации
│
├── 🛠️ Скрипты (Windows)
│   ├── docker-start.bat               # Запуск
│   ├── docker-stop.bat                # Остановка
│   ├── docker-restart.bat             # Перезапуск
│   └── docker-logs.bat                # Логи
│
├── 🛠️ Скрипты (Linux/macOS)
│   ├── docker-start.sh                # Запуск
│   └── docker-stop.sh                 # Остановка
│
├── 📋 Утилиты
│   ├── Makefile                       # Make команды
│   └── .gitignore                     # Git исключения
│
└── 📚 Документация
    ├── DOCKER_GUIDE.md                # Полное руководство (EN)
    ├── DOCKER_RU.md                   # Полная инструкция (RU)
    ├── ЗАПУСК_DOCKER.txt              # Быстрый старт (RU)
    └── DOCKER_FILES.md                # Этот файл
```

---

## ✅ Чек-лист первого запуска

- [ ] Docker Desktop установлен
- [ ] Docker Desktop запущен
- [ ] Открыт терминал/командная строка в папке проекта
- [ ] Выполнена команда: `docker-compose up -d`
- [ ] Открыт браузер: http://localhost:8001
- [ ] Приложение работает! 🎉

---

## 💡 Полезные советы

1. **Для разработки**: используйте `docker-compose.yml`
2. **Для продакшн**: используйте `docker-compose.prod.yml`
3. **Для быстрого запуска**: используйте `.bat` (Windows) или `.sh` (Linux/macOS) скрипты
4. **Для автоматизации**: используйте `Makefile`
5. **Для отладки**: `docker-compose logs -f` или `make logs`

---

## 🆘 Нужна помощь?

- **Краткая инструкция**: `ЗАПУСК_DOCKER.txt`
- **Подробная инструкция (RU)**: `DOCKER_RU.md`
- **Подробная инструкция (EN)**: `DOCKER_GUIDE.md`
- **Основная документация**: `README.md`

---

**Готово! 🚀 Все файлы для работы с Docker созданы!**

